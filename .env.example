# Supabase browser build variables. Use the publishable key only; never expose
# SUPABASE_SERVICE_ROLE_KEY or CLOUDINARY_API_SECRET to client code.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=<copy PUBLISHABLE_KEY from `npx supabase status -o env`>

# Supabase/Cloudflare Worker runtime variables and secrets.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=<copy PUBLISHABLE_KEY from `npx supabase status -o env`>
SUPABASE_SERVICE_ROLE_KEY=<copy SERVICE_ROLE_KEY from `npx supabase status -o env`>
SUPABASE_AUTH_ENABLED=true
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# Use `wrangler secret put` for these in Cloudflare.
CLOUDINARY_CLOUD_NAME=<cloudinary-cloud-name>
CLOUDINARY_API_KEY=<cloudinary-api-key>
CLOUDINARY_API_SECRET=<cloudinary-api-secret>

# Application/runtime.
APP_URL=http://localhost:3000
NODE_API_BASE_URL=http://127.0.0.1:8787/api/node
DEBUG_MODE=true
SESSION_SECRET=replace-with-a-long-random-secret

# Legacy compatibility. Existing local flows may still use these until all
# accounts have been moved to Supabase Auth.
AUTH_SECRET="changeme"
AUTH_URL="http://localhost:3000"
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Gmail API (transactional app email).
GMAIL_CLIENT_ID=""
GMAIL_CLIENT_SECRET=""
GMAIL_REFRESH_TOKEN=""
GMAIL_SENDER_EMAIL=""
GMAIL_ACCESS_TOKEN=""

# Gmail SMTP-compatible OAuth settings (the Cloudflare Worker uses Gmail REST API transport).
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_AUTH_TYPE="OAuth2"
SMTP_CLIENT_ID=""
SMTP_CLIENT_SECRET=""
SMTP_REFRESH_TOKEN=""
SMTP_ACCESS_TOKEN=""

# Optional integrations retained by existing routes.
REDIS_URL="redis://localhost:6379"
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
RESEND_API_KEY=""
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Admin bootstrap for the local seed script.
ADMIN_BOOTSTRAP_EMAIL="admin@edicut.com"
ADMIN_BOOTSTRAP_PASSWORD="change-this-long-admin-password"
ADMIN_BOOTSTRAP_NAME="EdiCut Admin"
